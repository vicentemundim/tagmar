<ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <li class="dropdown">
      <a href="" class="dropdown-toggle" data-toggle="dropdown">
        <%= current_user.name %>
        <b class="caret"></b>
      </a>

      <ul class="dropdown-menu">
        <li><%= link_to t('buttons.profile'), edit_user_registration_path %></li>
        <li class="divider"></li>
        <li><%= link_to t('buttons.sign_out'), destroy_user_session_path, method: :delete %></li>
      </ul>
    </li>
  <% else %>
    <li>
      <%= link_to t('buttons.sign_up'), new_user_registration_path %>
    </li>
    <li>
      <%= link_to t('buttons.sign_in'), new_user_session_path %>
    </li>
  <% end -%>
</ul>
